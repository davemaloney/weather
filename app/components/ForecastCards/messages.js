/*
 * ForecastCards Messages
 *
 * This contains all the text for the ForecastCards component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ForecastCards';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ForecastCards component!',
  },
});
