/*
 * UnitsSelect Messages
 *
 * This contains all the text for the UnitsSelect component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UnitsSelect';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UnitsSelect component!',
  },
});
